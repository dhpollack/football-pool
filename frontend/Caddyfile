localhost {
    route {
        handle /api/* {
            reverse_proxy {$VITE_BACKEND_URL:http://backend:8080}
        }

        handle {
            root * /usr/share/caddy
            try_files {path} /index.html
            file_server
        }
    }
    log
}
