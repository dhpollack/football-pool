import { Typography } from '@mui/material';

const HomePage = () => {
  return (
    <div>
      <Typography variant="h4">Welcome to the Football Pool!</Typography>
    </div>
  );
};

export default HomePage;